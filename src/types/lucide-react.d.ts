// Provide a lightweight module declaration to satisfy the TypeScript compiler
// when the package ships ESM .mjs artefacts that the language server can't
// automatically resolve in some environments.
declare module 'lucide-react'
